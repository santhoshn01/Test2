plugins {
    id 'java'
    id 'com.jfrog.artifactory' version "4.26.2"
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}

artifactory {
    contextUrl = 'https://192.168.100.62:8082/artifactory/libs-release-local'
    publish {
        repository {
            username = 'admin'
            password = 'Chella*123'
        }
    }
}
